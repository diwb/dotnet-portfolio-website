# GitHub Pages

The workflow `.github/workflows/pages.yml` builds the site and publishes `/out` using `actions/upload-pages-artifact` and `actions/deploy-pages`.

Repository settings should enable GitHub Pages through GitHub Actions.
