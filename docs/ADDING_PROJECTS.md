# Adding Projects

1. Add the project entry to `src/data/projects.ts`.
2. Keep the repository under `https://github.com/diwb/`.
3. Add only verified scores, tests, coverage and release evidence.
4. Run `npm run validate:data`.
5. Run `npm run audit:claims`.
6. Run tests and static export before release.
