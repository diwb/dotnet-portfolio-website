# Deployment

Build command:

```bash
npm run build
```

Output directory:

```text
out
```

The output can be hosted on static platforms or uploaded to shared hosting. No server runtime is required after export.
